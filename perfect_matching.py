def perfectmatching(a,b,c,d):
	a=int(a)
	b=int(b)
	c=int(c)
	d=int(d)
	B1vals=[a,b]
	B2vals[c,d]
	if a>=b and d>=c:
		print "B1 gets S1, B2 gets S2, prices stay at zero"
	elif b>=a and c>=d:
		print "B1 gets S2, B2 gets S1, prices stay at zero"
	else:
		while 
